import React, { useContext } from "react";
import AddNote from "./AddNote";
import Note from "./Note";


const Home = () => {
  return (
    <div>
      <Note />
    </div>
  );
};

export default Home;
